# Core
PORT=5000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/apiguard
JWT_SECRET=changeme
# Generate 32-byte hex: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY=

# GitHub App (optional)
GITHUB_APP_ID=
# Paste full PEM with newlines preserved; for env files escape newlines as \n
GITHUB_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=

# GitHub OAuth (optional)
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# Alerts (optional)
SENDGRID_API_KEY=
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_SIGNING_SECRET=
SLACK_BOT_TOKEN=

# LLM (optional)
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_MAX_TOKENS=1000
LLM_TIMEOUT=10000

